# ADRs

Architecture Decision Records (ADRs) capture decisions that are costly to reverse.

## Index

- `0001-desktop-window.md`: Switch from menu bar UI to persistent desktop window

## Template

Use `template.md`.

